# Supabase configuration for Zero to AI Hub
# Copy this file to .env and fill in the values from your Supabase project.

# Public anon key from Settings > API > Project API keys
# Required to enable the hosted Supabase project integration. When this value is
# present the Vite dev server automatically exposes it to the browser build.
SUPABASE_KEY=

# Service role key from Settings > API > Project API keys
# Required by the migration script to push the database schema
SUPABASE_SERVICE_ROLE_KEY=

# Optional override for the hosted Supabase project URL. When filled the value is
# mirrored to VITE_SUPABASE_URL automatically.
SUPABASE_URL=

# Optional overrides for local development builds that still rely on Vite env vars.
# In most cases these can stay empty because values above are mirrored.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# WhatsApp contact used on the dashboard while o pagamento manual é confirmado.
# Informe apenas números com DDI/DDI (ex: 5511999999999). O link é exibido aos alunos
# enquanto aguardam a liberação manual.
VITE_WHATSAPP_CONTACT=

# Mensagem opcional pré-preenchida ao abrir o WhatsApp.
# Ajuste o texto para explicar como solicitar a liberação do acesso.
VITE_WHATSAPP_CONTACT_MESSAGE="Olá! Meu pagamento já foi confirmado e preciso da liberação da Área do Aluno."
